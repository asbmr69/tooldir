<p style="color: green"><%= notice %></p>

<%= render @tool %>

<div>
  <%= link_to "Edit this tool", edit_tool_path(@tool), class:"btn btn-secondary" %> |
  <%= link_to "Back to tools", tools_path, class:"btn btn-secondary" %>

  <%= button_to "Destroy this tool", @tool, method: :delete %>
</div>
